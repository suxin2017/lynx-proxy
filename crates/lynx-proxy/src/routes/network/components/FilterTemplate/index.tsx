import { Button, Dropdown } from 'antd';
import React from 'react';

export const FilterTemplate: React.FC = () => {
  return (
    <Dropdown>
      <Button>Filter Template</Button>
    </Dropdown>
  );
};
